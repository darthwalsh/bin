## State
react state in the URL query (could be fragment)
- [ ] [An alternative to handle state in React: the URL ! - DEV Community](https://dev.to/gaels/an-alternative-to-handle-global-state-in-react-the-url--3753) 
## Classes
Maybe try https://react.gg/ $245 or $375 for full class

## Inspecting Element When Click Updates State
Many e.g. dropdowns are fragile around focus/click-away behavior, so inspection can accidentally trigger the same close code path as a real click. Pressing `F8` to pause JS is usually the fastest workaround.