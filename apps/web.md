See [[Stack]] for the 8-layer taxonomy (hosting → web server → runtime → framework → API → database → UI → build tools), technology lists, and named stacks timeline.

Each layer has a distinct job. Mixing concerns (e.g. letting your build tool decide your hosting, or your app framework serve production traffic) introduces [[code.health#Coupling|coupling]].
