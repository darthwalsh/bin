[project]
name = "bench-deps"
version = "0.0.1"
requires-python = ">=3.9"
dependencies = ["requests"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.envs.default]
skip-install = true
dependencies = ["requests"]
